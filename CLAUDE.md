# Trove — Claude Code Project Instructions

See [README.md](README.md) for project overview and [AGENTS.md](AGENTS.md) for AI agent usage guidance.

## Development Notes

- Shell utilities live in `lib/` and are sourced via the `install` script
- Tests are in `tests/` — run with `bash tests/run_tests.sh`
- User config overrides go in `config/trove.local.conf` (gitignored)
